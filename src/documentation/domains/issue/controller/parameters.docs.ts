/**
 * @swagger
 * components:
 *   parameters:
 *     IssueIdParamDTO:
 *       in: path
 *       name: issueId
 *       required: true
 *       schema:
 *         type: string
 *         format: uuid
 *       description: The issue id to retrieve its worked time.
 * */
